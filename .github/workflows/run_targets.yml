on:
    push:
        branches:
            - main
            - master
    pull_request:
        branches:
            - main
            - master

name: Run-Targets

jobs:
    Run-Temp-Logging:
        runs-on: ubuntu-latest
        steps:
            - run: docker run --rm -e BUCKET_NAME={{ secrets.BUCKET_NAME }}  docker.io/jaredlander/templogging:latest
